# 🚀 UpdateManager 系統更新模組說明文件

本模組提供 ESP32 的 **GitHub HTTPS 韌體升級 (OTA)**、**SD 卡圖片串流下載** 與 **版本控制 (Flash NVS)**。

---

## 一、 架構與職責分工 (Architecture)

採用高解耦結構設計，各元件職責如下：

| 元件名稱 | 核心職責 | 運作機制與 UI 特性 |
| :--- | :--- | :--- |
| **`UpdateManager`** | 版本比對與流程總管 | 請求 JSON，比對 `firmware_version` 與 Flash NVS `asset_version` |
| **`GitHubOTA`** | HTTPS 韌體燒錄 | 處理 GitHub 302 重定向與燒錄，綁定 TFT 顯示**綠色進度條**，完成自動重啟 |
| **`ImageDownloader`** | SD 卡圖片串流下載 | 以 512-Byte 緩衝區串流寫入 SD 卡。支援前台（**藍色進度條**）與背景無 UI 下載 |

> 🔒 **Flash NVS 原子寫入機制**：
> 在包含多張圖片的大更新中，`UpdateManager` 會使用迴圈下載清單圖片。**唯有所有圖片 100% 下載成功**，才會將新版本號寫入 Flash NVS。若下載中斷，Flash 維持舊版號，下次開機自動重試。

---

## 二、 核心運作邏輯 (Workflow)

```text
 ┌─────────────────────────────────────────────────────────────┐
 │                    UpdateManager::checkMajorUpdates()        │
 └──────────────────────────────┬──────────────────────────────┘
                                │ GET /api/v1/config
                                ▼
 ┌─────────────────────────────────────────────────────────────┐
 │                    FastAPI JSON 系統配置                     │
 └──────────────┬───────────────────────────────┬──────────────┘
                │                               │
    [ 韌體版本號比對 ]                 [ 圖片資產版本號比對 ]
   (firmware_version)                   (Server > Flash NVS)
                │                               │
                ▼                               ▼
    ┌───────────────────────┐       ┌───────────────────────┐
    │  GitHubOTA::startOTA  │       │  ImageDownloader 迴圈 │
    │   (綠色 UI 進度條)     │       │   (藍色 UI 進度條)    │
    └───────────────────────┘       └───────────┬───────────┘
                                                │ 全部圖片成功 (100%)
                                                ▼
                                    ┌───────────────────────┐
                                    │ commitAssetVersion()  │
                                    │ (寫入 Flash NVS 記憶) │
                                    └───────────────────────┘